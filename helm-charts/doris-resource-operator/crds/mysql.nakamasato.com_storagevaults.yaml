---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: storagevaults.mysql.nakamasato.com
spec:
  group: mysql.nakamasato.com
  names:
    kind: StorageVault
    listKind: StorageVaultList
    plural: storagevaults
    singular: storagevault
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.reason
      name: Reason
      type: string
    - jsonPath: .status.vaultCreated
      name: VaultCreated
      type: boolean
    - jsonPath: .status.isDefault
      name: IsDefault
      type: boolean
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: StorageVault is the Schema for the storagevaults API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StorageVaultSpec defines the desired state of StorageVault
            properties:
              clusterName:
                description: Cluster name to reference to, which decides the destination
                type: string
                x-kubernetes-validations:
                - message: Cluster name is immutable
                  rule: self == oldSelf
              isDefault:
                default: false
                description: IsDefault indicates whether this vault should be set
                  as the default storage vault
                type: boolean
              name:
                description: Name of the storage vault
                maxLength: 64
                pattern: ^[a-zA-Z][a-zA-Z0-9_]*$
                type: string
              s3Properties:
                description: S3Properties defines the properties for S3-compatible
                  storage vault
                properties:
                  accessKeySecretRef:
                    description: AccessKeySecretRef is the secret reference for the
                      S3 access key
                    properties:
                      key:
                        type: string
                      name:
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  bucket:
                    description: Bucket is the bucket of your object storage account
                    type: string
                  endpoint:
                    description: Endpoint is the endpoint used for object storage
                      (without http:// or https://)
                    type: string
                  provider:
                    description: Provider is the cloud vendor providing the object
                      storage service
                    enum:
                    - S3
                    - OSS
                    - COS
                    - OBS
                    - BOS
                    - AZURE
                    - GCP
                    type: string
                  region:
                    description: Region is the region of your bucket
                    type: string
                  rootPath:
                    description: RootPath is the path where the data would be stored
                    type: string
                  secretKeySecretRef:
                    description: SecretKeySecretRef is the secret reference for the
                      S3 secret key
                    properties:
                      key:
                        type: string
                      name:
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  usePathStyle:
                    default: true
                    description: UsePathStyle indicates whether to use path-style
                      URL
                    type: boolean
                required:
                - bucket
                - endpoint
                - provider
                - region
                - rootPath
                type: object
              type:
                description: Type of storage vault (only S3 is supported)
                enum:
                - S3
                type: string
                x-kubernetes-validations:
                - message: HDFS storage vault is not supported yet
                  rule: self != 'HDFS'
            required:
            - clusterName
            - name
            - type
            type: object
          status:
            description: StorageVaultStatus defines the observed state of StorageVault
            properties:
              isDefault:
                description: IsDefault indicates whether this vault is set as the
                  default storage vault
                type: boolean
              phase:
                description: Phase represents the current phase of the storage vault
                type: string
              reason:
                description: Reason provides more information about the current phase
                type: string
              vaultCreated:
                description: VaultCreated indicates whether the vault has been created
                  in Doris
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
